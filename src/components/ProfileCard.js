import React, { useState } from 'react';

const ProfileCard = () => {
    const[edit,SetEdit]=useState(false);

    return (
        <div>
            
        </div>
    );
};

export default ProfileCard;
